function add(n1, n2) {
    // if (typeof(n1) !== 'number' || typeof(n2) !== 'number') {
    //   throw new Error('入力値は正しくありません');
    // }
    return n1 + n2;
}
{
    var number1 = 42;
    var number2 = 4.2;
    var result = add(number1, number2);
    console.log(result);
}
